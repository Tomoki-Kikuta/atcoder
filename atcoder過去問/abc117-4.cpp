#include<iostream>
#include<algorithm>
#include<vector>
#include<math.h>
#define MAX 100005
using namespace std;

int main(void){
    int N;
    long int K,A[MAX];
    cin >> N >> K;
    for(int i=0;i<N;i++){
        cin >> A[i];
    }
    vector<long int> number_;
    for(int i=0;i<N;i++){
        number_.push_back(number);
    }
    return 0;
}